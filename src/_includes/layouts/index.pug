include ../mixins/page-mixins.pug

doctype html
html(lang="en")
    include head.pug
    body
        +contact-button
        include contact.pug

        +theme-control
        
        main#swup.transition-main
            +logo
            | !{content}
        
            include footer.pug